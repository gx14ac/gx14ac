import { fira } from "@utils/font";

export default function Header() {
    return (
        <div>
            <p className={`${fira.className} text-xl`}>
                gx14ac
            </p>
        </div>
    )
}